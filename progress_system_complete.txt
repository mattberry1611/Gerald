TASK PROGRESS SYSTEM — COMPLETE
================================
Date: 2026-06-16
Version: V1.4

IMPLEMENTATION:
---------------
File: gerald_app/lib/providers/app_state.dart
  TaskStage enum:
    none, queued, sending, accepted, working, reviewing, finalising, complete, error

  Fields:
    _taskStage: TaskStage
    _taskStartTime: DateTime?
    _completeTimer: Timer?
    _elapsedTimer: Timer? (1s periodic, notifies listeners for live elapsed)

  Getters:
    taskStage, taskProgress (0-100), taskStageName
    taskElapsed (Duration), hasActiveTask, isLongTask (>120s)

  Stage transitions:
    sendPrompt() → busy:         stage = queued (5%)
    _executePrompt() start:      stage = sending (15%), startTime = now
    POST /start returns ok:      stage = accepted (25%)
    _poll() sees executing:      stage = working (45%)
    _poll() sees awaiting:       stage = reviewing (75%)
    _readResult() entry:         stage = finalising (90%)
    result displayed:            stage = complete (100%), resets after 3s
    any error:                   stage = error, resets after 5s

File: gerald_app/lib/widgets/status_panel.dart
  _TaskProgressSection widget (shown when hasActiveTask):
    - LinearProgressIndicator (3px, rounded corners)
    - Colors: blue (active), green (complete), red (error)
    - Stage dot + stage label + percentage
    - Elapsed time (right-aligned, monospace)
    - Long task warning row (amber, clock icon, after 120s)

STAGES:
-------
Queued ............... 5%
Sending ............. 15%
Accepted ............ 25%
Claude Working ...... 45%
Reviewing ........... 75%
Finalising .......... 90%
Complete ........... 100%

FEATURES:
---------
✓ 7-stage progress bar with percentages
✓ Live stage label (updates with each transition)
✓ Elapsed timer (1-second updates)
✓ Long task warning after 120 seconds
✓ Error state (red bar, "Error" label)
✓ Auto-resets 3s (success) / 5s (error) after completion

BUILD: ✓ flutter build apk --debug — PASSES
